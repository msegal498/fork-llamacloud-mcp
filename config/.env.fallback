# PDF Chunking System Fallback Configuration
# This configuration is for local testing without API keys

# Server Ports
FRONTEND_PORT=8080
MCP_SERVER_PORT=8000

# Server Names and URLs
MCP_SERVER_NAME=pdf-chunking-server
MCP_SERVER_URL=http://localhost:8000

# LlamaCloud Configuration (Not used in fallback mode)
LLAMA_CLOUD_API_KEY=not-used
LLAMA_CLOUD_ORG_ID=not-used
LLAMA_CLOUD_PROJECT_NAME=not-used
LLAMA_CLOUD_INDEX_NAME=not-used

# OpenAI Configuration (Not used in fallback mode)
OPENAI_API_KEY=not-used

# PDF Processing Directories
PDF_UPLOAD_DIR=./data/uploads
PDF_OUTPUT_DIR=./data/outputs

# PDF Processing Configuration
DEFAULT_CHUNK_SIZE=1000
DEFAULT_CHUNK_OVERLAP=200
